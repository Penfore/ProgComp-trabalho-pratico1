#pragma once

// Definindo os protótipos para as funções escritas no arquivo genetico.cpp
bool avaliação(unsigned short);
unsigned short cruzamento_ponto_unico(unsigned short, unsigned short);
unsigned short cruzamento_aritmetico(unsigned short, unsigned short);
unsigned short mutação_simples(unsigned short);
unsigned short mutação_dupla(unsigned short);
