#pragma once

// Definindo os protótipos para as funções escritas no arquivo binario.cpp
unsigned short ligar_bit(unsigned short, unsigned char);
unsigned short desligar_bit(unsigned short, unsigned char);
bool testar_bit(unsigned short, unsigned char);
unsigned short and_binario(unsigned short, unsigned short);
unsigned short or_binario(unsigned short, unsigned short);
unsigned short bits_baixos(unsigned short);
unsigned short bits_altos(unsigned short);
